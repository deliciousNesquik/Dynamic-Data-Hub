<Application x:Class="DynamicDataHub.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DynamicDataHub"
             StartupUri="Views/DDHManagerWindow.xaml">
    <Application.Resources>

        <!--Шрифт для всего приложения-->
        <Style TargetType="{x:Type Control}">
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        
        <!--Элементы управления-->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#4285F4"></Setter>
        </Style>
        <Style TargetType="ListView">
            <Setter Property="Background" Value="#121212"></Setter>
        </Style>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="#E0E0E0"/>
        </Style>

        <!--Окна и рамки-->
        <Style TargetType="Grid">
            <Setter Property="Background" Value="#121212"></Setter> <!--#FFFFFF-->
        </Style>
        <Style TargetType="Frame">
            <Setter Property="BorderBrush" Value="White"/>
        </Style>
    </Application.Resources>
    
</Application>
